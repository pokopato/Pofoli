<h1>プロフィール編集ページ</h1>

<%= form_for(@user) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.label :email %>
  <%= f.text_field :email %>
  
  <%= f.submit '更新！', class: 'btn btn-lg btn-primary' %>
<% end %>